{
  "name": "tsdl",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "lint": "eslint && prettier . -w",
    "release": "npm run build && npx lerna publish --no-private",
    "prepare": "husky install && npx lerna link",
    "build": "npx lerna run build",
    "readme": "ts-node scripts/compileReadme.ts"
  },
  "private": true,
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.59.1",
    "@typescript-eslint/parser": "^5.0.1",
    "eslint": "^8.7.0",
    "husky": "^8.0.0",
    "lerna": "6.0.1",
    "nx": "16.0.1",
    "nx-cloud": "latest",
    "prettier": "^2.5.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "workspaces": [
    "packages/*",
    "tests/*",
    "playground/*"
  ],
  "author": "Jonathan Asplund <jonathan@asplund.net>"
}