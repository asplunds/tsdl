## Installation

TSDL is installed on both your client and your server

### Server

import { Tab, Tabs } from "nextra-theme-docs";

<Tabs items={["pnpm", "npm", "yarn"]}>
  <Tab>
    ```shell filename="example-app/server" copy
    pnpm add @tsdl/server @tsdl/core
    ```
    ### Client

    <Tabs items={["vanilla", "React query", "SWR"]}>
      <Tab>

        ```shell filename="example-app/client" copy
        pnpm add @tsdl/client @tsdl/core
        ```

      </Tab>
      <Tab>

        ```shell filename="example-app/client" copy
        pnpm add @tsdl/react-query @tsdl/core @tanstack/react-query
        ```

      </Tab>
      <Tab>

        ```shell filename="example-app/client" copy
        pnpm add @tsdl/swr @tsdl/core swr
        ```
      </Tab>
    </Tabs>

  </Tab>
  <Tab>
    ```shell filename="example-app/server" copy
    npm i @tsdl/server
    ```
    ### Client
    <Tabs items={["vanilla", "React query", "SWR"]}>
      <Tab>

        ```shell filename="example-app/client"
        npm i @tsdl/client @tsdl/core
        ```

      </Tab>
      <Tab>

        ```shell filename="example-app/client" copy
        npm i @tsdl/react-quer @tsdl/corey @tanstack/react-query
        ```

      </Tab>
      <Tab>

        ```shell filename="example-app/client" copy
        npm i @tsdl/swr @tsdl/core swr
        ```
      </Tab>
    </Tabs>

  </Tab>
  <Tab>
    ```shell filename="example-app/server" copy
    yarn add @tsdl/server
    ```
    ### Client
    <Tabs items={["vanilla", "React query", "SWR"]}>
      <Tab>

        ```shell filename="example-app/client" copy
        yarn add @tsdl/client @tsdl/core
        ```

      </Tab>
      <Tab>

        ```shell filename="example-app/client" copy
        yarn add @tsdl/react-query @tsdl/core @tanstack/react-query
        ```

      </Tab>
      <Tab>

        ```shell filename="example-app/client" copy
        yarn add @tsdl/swr @tsdl/core swr
        ```
      </Tab>
    </Tabs>

  </Tab>
</Tabs>
